<div class="container p-3 my-3 border" style="background-color: lightsteelblue; vertical-align: top;">

    <div class="card-block" style="background-color: lightsteelblue; vertical-align: top;">
        <div class="row">
            <div class="col-md-6">	

                <h1>Interventions#new</h1>
                <br>               

                <%# <%=select_tag(:id, options_from_collection_for_select(Employee.all, :user_id, :first_name), include_blank: true) %>
                <br>
                <%= label_tag(:id, "Select a Employee:") %>
                <%= collection_select(:id, nil, Employee.all, :user_id, :first_name, include_blank: true) %>                
                <br>
                <%= label_tag(:id, "Select a Customer:") %>
                <%= collection_select(:id, nil,  Customer.all, :user_id, :company_name, include_blank: true,) %>
                <br>
                <%= label_tag(:id, "Select a Building:") %>
                <%= collection_select(:id, nil,  Building.all, :id, :id, prompt: "Select something") %>
                <br>
                <%= label_tag(:id, "Select a Battery:") %>
                <%= collection_select(:id, nil,  Battery.all, :id, :id, include_blank: true) %>
                <br>
                <%= label_tag(:id, "Select a Column:") %>
                <%= collection_select(:id, nil,  Column.all, :id, :id, include_blank: true) %>
                <br>
                <%= label_tag(:id, "Select a Elevator:") %>
                <%= collection_select(:id, nil,  Elevator.all, :serial_number, :serial_number, include_blank: true) %>

            </div>

                        
        </div>		
    </div>				
</div>

